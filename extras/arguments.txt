char	**ft_arguments(char *args);
{
	int i;
	int j;
	int double_quote;
	int	single_quote;

	i = -1;
	j = 0;
	double_quote = 0;

	if ft_split finds " or ';

	while (args[++i])
	{
		if (args[i] == '\"' && single_quote % 2 == 0)
			double_quote++;
		else if (args[i] == '\'' && double_quote % 2 == 0)
			single_quiote++;
	}
	if (single_quote % 2 != 0 || double_quote % 2 != 0)
		ft_error_exit("Error qutes"); // special case "'"' //




}
